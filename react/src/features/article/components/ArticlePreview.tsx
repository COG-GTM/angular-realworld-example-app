// STUB — owned by the "home" child session.
import type { Article } from '../../../types';
export function ArticlePreview({ article: _article }: { article: Article }) {
  return <div className="article-preview" />;
}
