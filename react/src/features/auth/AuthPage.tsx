// STUB — owned by the "auth" child session. Replace with the migrated login/register page.
export function AuthPage({ authType }: { authType: 'login' | 'register' }) {
  return <div className="auth-page" data-stub={authType} />;
}
